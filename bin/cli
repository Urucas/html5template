#!/usr/bin/env node

var html5template = require('../node');
var args = process.argv.splice(2, process.argv.length);
var params = {};
for(var i=0; i<args.length;i+=2){
	params[args[i].replace(/-/g,'')] = args[i+1];
}
html5template(params);
